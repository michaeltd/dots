#!/bin/bash
#
# ~/bin/mxcl
# e[M]a[X] [C][L]ient

if [[ -n "${DISPLAY}" ]]; then
    emacsclient --alternate-editor="emacs" --create-frame "${@}" &
else
    emacsclient --alternate-editor="emacs" --tty "${@}" &
fi
