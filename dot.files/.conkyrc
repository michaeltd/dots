conky.config = {
-- Check http://conky.sourceforge.net/config_settings.html for the list of settings
-- Check http://conky.sourceforge.net/variables.html for the list of variables

-- - Conky settings - #
	update_interval = 1,
	total_run_times = 0,
	net_avg_samples = 1,
	cpu_avg_samples = 1,
	if_up_strictness = 'link',

	imlib_cache_size = 0,
	double_buffer = true,
	no_buffers = true,
	background = true,
	format_human_readable = true,

-- - Text settings - #
	use_xft = true,
	font = 'Ubuntu:style=regular:size=10',
	xftalpha = 0.8,
	override_utf8_locale = false,
	text_buffer_size = 4096,
	short_units = true,

-- - Window specifications - #
	own_window_class = 'Conky',
	own_window = true,
	own_window_type = 'normal',
	own_window_transparent = true,
	own_window_argb_visual = true,
--	own_window_colour 3e3e3e #FFA300 151515 082103 331f00,
--	own_window_argb_value 200,
--	own_window_hints = 'sticky,skip_taskbar,skip_pager',
--	own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
	own_window_hints = 'undecorated,sticky,skip_taskbar,skip_pager',
	alignment = 'top_right',
	gap_x = 64,
	gap_y = 64,
	minimum_width = 510, minimum_height = 150,
	maximum_width = 510,
	max_text_width = 0,

-- - Graphics settings - #
	no_buffers = true,
	uppercase = false,
	cpu_avg_samples = 2,
	net_avg_samples = 2,
	use_spacer = 'none',
	top_name_width = 18,
	top_cpu_separate = false,

	draw_shades = false,
	draw_borders = false,
	stippled_borders = 1,
	draw_graph_borders = true,
	stippled_borders = 5,
--border_inner_margin 0
--border_outer_margin 0
	border_width = 1,
	default_color = 'white',
	default_shade_color = 'black',
	draw_outline = false,
	default_outline_color = 'black',

};

conky.text = [[
# TIME
${color FFA300}${font Ubuntu:pixelsize=120}${time %H:%M}${font}${voffset -84}${offset 10}${color EAEAEA}${font Ubuntu:pixelsize=42}${time %d} ${voffset -15}${color FFA300}${font Ubuntu:pixelsize=22}${time  %B} ${time %Y}${font}${voffset 24}${color EAEAEA}${font Ubuntu:pixelsize=58}${offset -200}${time %A}${font}
${font Ubuntu:pixelsize=10}${color FFA300} HD:${color}${fs_free /}/${fs_size /}${color FFA300} RAM:${color}${mem}/${memmax} ${color FFA300} CPU:${color}${cpu cpu0}%${color FFA300} UT: ${color}${uptime_short}${font}${font Ubuntu:pixelsize=10}${color FFA300} BT:${color}${battery}${battery_bar 6,20}${color FFA300} REM:${color}${battery_time}${font}
]];
